<!--
════════════════════════════════════════════════════════════════════════════════
  HARNESS KNOWLEDGE DOCUMENT — 작성 규칙
  이 파일은 Developer 노드의 user message에 고신뢰 레퍼런스로 직접 주입됩니다.
════════════════════════════════════════════════════════════════════════════════

이 파일은 context/phases/<phase>.md의 technology 필드로 연결됩니다.
  예: - **technology**: `emqx`  →  context/knowledge/emqx.md 주입

─── A. 파일명 규칙 ────────────────────────────────────────────────────────────
  · <technology-name>.md              ← 기술 베이스라인 (이 파일 형식)
  · <technology-name>-llm-findings.md ← Runtime Verifier가 자동 생성. 수동 편집 금지.

─── B. 내용 원칙 ──────────────────────────────────────────────────────────────
  · 검증된 설정값만 기재 (이미지 버전, Helm values 키, YAML 예시)
  · 이 환경(GikView)에 특화된 주의사항 중심으로 작성
  · 기술 공식 문서와 충돌하는 내용은 이 파일이 우선 (환경 특화 오버라이드)
  · 추측이나 실험 중인 내용은 기재 금지 → llm-findings.md가 그 역할을 담당

─── C. 금지 항목 ──────────────────────────────────────────────────────────────
  · "권장", "고려", "검토 필요" 등 모호한 표현 금지
  · 검증되지 않은 설정값 기재 금지
  · phase 문서 내용 중복 기재 금지 (환경 특화 제약은 phase 문서 §4에 기재)
  · 기술 일반론 나열 금지 — 이 환경에서 실제로 문제가 됐거나 중요한 항목만

════════════════════════════════════════════════════════════════════════════════
-->

# <Technology> Knowledge

> 이 문서는 Developer 노드에게 고신뢰 레퍼런스로 주입됩니다.
> 모든 설정값은 이 환경(GikView)에서 검증된 것이어야 합니다.

## 이미지 / 버전

**채택**: `<image>:<tag>` (`<full-registry-path>`)

<버전 선택 근거 — 1~3문장. 예: 라이선스 변경, EOL 일정, 호환성 이슈>

Helm chart: `<repo>/<chart>` `<chart-version>` (repo: `<repo-url>`)

## 주요 설정

<이 환경에서 반드시 올바르게 설정해야 하는 값들. YAML 예시 포함.>

```yaml
# values.yaml (공통)
key: value

# values-dev.yaml
key: dev-value

# values-prod.yaml
key: prod-value
```

## 알려진 주의사항

- <이 기술의 알려진 트랩 또는 k8s 환경 특화 제약>
- <예: StatefulSet 재시작 시 데이터 불일치 조건, downward API 필수 항목>

## 환경별 분리 필요 항목

| 항목 | dev | prod |
|------|-----|------|
| `<values-key>` | `<dev-value>` | `<prod-value>` |
