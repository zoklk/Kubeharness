# 환경 / 시크릿
.env

# Python 가상환경
.venv/

# Python 캐시
__pycache__/
*.pyc
*.pyo
*.pyd

# 빌드 산출물
dist/
build/
*.egg-info/
*.egg

# 테스트 / 린터 캐시
.pytest_cache/
.mypy_cache/
.ruff_cache/
.coverage
htmlcov/

# 런타임 로그 (구조는 유지, 내용은 제외)
logs/raw/*
!logs/raw/.gitkeep

context/

# 환경별 클러스터 설정 (kubeconfig 경로 등 로컬 환경 종속)
# kubeconfig경로를 담기위한 파일이지만 현재 코드에서는 .kube에서 직접 참조중
config/cluster.yaml

# 임시 스크립트
check_gemini_model.py